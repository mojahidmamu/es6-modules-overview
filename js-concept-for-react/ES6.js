const numbers = [45, 98, 78];
const student = {
  name: "mojahid",
  age: 15,
  movie: ["tofan", "dakar", "pola"],
};
// 1.templete string
const about = `my name is ${student.name} and age of ${student.age} has number ${numbers[2]} also likes movie is '  ${student.movie[0]}'`;
console.log(about);

// 2.arrow function
const getFixed = () => 55;
const getSixty = () => 65;

// spread operator

